# Web Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Computer Vision - let surya control opencv version
numpy>=1.24.0,<2.0.0

# OCR - Surya requires PyTorch 2.7+
# Use CUDA 12.1 compatible PyTorch (latest available)
--extra-index-url https://download.pytorch.org/whl/cu121
torch>=2.7.0
torchvision>=0.22.0

# Surya OCR - Primary engine (state-of-art for scene text)
surya-ocr>=0.17.0
# Pin transformers to <5.0 for Surya compatibility
transformers>=4.36.0,<5.0.0

# EasyOCR - Fallback engine
easyocr==1.7.1

# PaddleOCR - Fast GPU-accelerated OCR (primary for killfeed)
# Using CUDA 12.x compatible version
# Pin to 2.7.x for stable API compatibility with paddlepaddle 2.6.x
paddlepaddle-gpu>=2.6.0,<3.0.0
paddleocr>=2.7.0,<3.0.0

# Database
psycopg2-binary==2.9.9

# Task Queue (for background processing)
celery[redis]==5.3.6
redis==5.0.1

# Data validation
pydantic==2.5.3
pydantic-settings==2.1.0

# Utilities
python-dotenv==1.0.0
aiofiles==23.2.1
httpx==0.26.0

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3

# Tracking/Filtering
filterpy==1.4.5
scipy>=1.11.0

requests